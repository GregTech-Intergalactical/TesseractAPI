package tesseract.api.rf;

import tesseract.api.DataHolder;

public class RFDataHolder extends DataHolder<Long, Long> {
    public RFDataHolder(Long immutableData, Long data) {
        super(immutableData, data);
    }
}
